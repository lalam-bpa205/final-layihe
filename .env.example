# ŞABLON — bunu ".env" adı ilə kopyalayıb öz dəyərlərinizi yazın.
# Həqiqi .env faylı git-də izlənmir.
DB_PASSWORD=guclu_bir_parol_yazin
# ən azı 64 simvol təsadüfi açar — məs: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=en_azi_64_simvol_tesadufi_acar_buraya
